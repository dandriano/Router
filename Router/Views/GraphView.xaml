<UserControl x:Class="Router.Views.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Router.ViewModels"
             xmlns:graph="http://schemas.panthernet.ru/graphx/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:GraphViewModel />
    </UserControl.DataContext>
    
    <Grid>
        <Grid>
            <graph:ZoomControl graph:ZoomControl.ViewFinderVisibility="Collapsed" Content="{Binding}" />
        </Grid>
    </Grid>
</UserControl>
