namespace Router.Wasm
{
    /// <summary>
    /// Visual settings for a graph: colors, paths, etc
    /// </summary>
    public class NetworkSettings
    {
        public static NetworkSettings Default => new NetworkSettings();
    }
}